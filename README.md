# Homebrew tap for IMM

Install IMM on macOS:

```bash
brew tap susu3304/imm
brew install imm
imm --version
```

The formula is updated automatically by
[`susu3304/imm-installers`](https://github.com/susu3304/imm-installers) when a
new IMM installer release is published.
